{
  "private": true,
  "scripts": {
    "dev": "docker compose up --build -d",
    "prod": "docker compose -f docker-compose.yml up --build",
    "down": "docker compose down",
    "unpublish-identity": "npm --prefix backend run unpublish-identity --"
  }
}
